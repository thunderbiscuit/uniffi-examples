namespace calendar {};

interface Calendar {
  constructor(i32 id, Clock clock);

  string get_info();

  i32 add(i32 a, i32 b);
};

[External="clock_ffi"]
typedef extern Clock;

[External="clock_ffi"]
typedef extern ClockType;
